= simple_form_for current_customer, url: :confirm_phone_profile, turboboost: true, html: {id: 'confirm-phone'} do |f|
  .row
    .col-sm-12
      = f.input :sms_confirmation_pin, label: 'Please enter the code you received:', wrapper: :vertical_input_group, error: true do
        = f.input_field :sms_confirmation_pin, class: 'form-control', as: :integer
        span.input-group-btn
          = f.button :submit, 'Verify'
